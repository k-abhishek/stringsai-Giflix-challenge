const config = {
    appName: 'Strings.ai Giflix',
    homepage: '/',
    primaryColor: '#e50914',
    url: 'http://api.giphy.com/v1/gifs'
  }
  //Url: typeof window === 'undefined' || window.location.hostname === 'localhost' ? '' : '//' + window.location.hostname
export default config
export { config }
  